# Frontend (Phase 1)

React + TypeScript + Vite + Tailwind CSS.

## Commands

```powershell
npm install
npm run dev      # http://localhost:5173
npm run build
npm run preview
```

## Environment

Copy `.env.example` to `.env` if you need to point at a non-default API URL.
